Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 DEPLOYMENT SUMMARY

{{- if .Values.ingress.enabled }}

✅ Ingress is ENABLED

Application URL(s):
{{- range .Values.ingress.hosts }}
  {{- if eq $.Values.ingress.tls.mode "none" }}
  🌐 http://{{ .host }}
  {{- else }}
  🔒 https://{{ .host }}
  {{- end }}
{{- end }}

{{- else }}

⚠️  Ingress is DISABLED

To access your application, you can port-forward:

  Frontend:
  $ kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "semaphore-chat.fullname" . }}-frontend {{ .Values.frontend.service.port }}:{{ .Values.frontend.service.port }}
  Then visit: http://localhost:{{ .Values.frontend.service.port }}

  Backend API:
  $ kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "semaphore-chat.fullname" . }}-backend {{ .Values.backend.service.port }}:{{ .Values.backend.service.port }}
  Then visit: http://localhost:{{ .Values.backend.service.port }}/api/health

{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 CONFIGURATION

Database:
{{- if .Values.postgresql.bundled }}
  ✅ PostgreSQL deployed (bundled)
     Database: {{ .Values.postgresql.auth.database }}
{{- else }}
  🔗 External PostgreSQL
     URI: (configured via postgresql.external.uri - check values for details)
{{- end }}

Cache:
{{- if .Values.redis.bundled }}
  ✅ Redis deployed (bundled)
     Host: {{ include "semaphore-chat.redis.host" . }}
{{- else }}
  🔗 External Redis
     Host: {{ .Values.redis.external.host }}:{{ .Values.redis.external.port }}
{{- end }}

LiveKit:
  🎥 External LiveKit Server
     URL: {{ .Values.livekit.url }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  SECURITY REMINDERS

{{- if eq .Values.secrets.jwtSecret "CHANGE-ME-long-random-secret-for-jwt-signing" }}

🚨 CRITICAL: You are using default JWT secrets!
   Please set custom values:

   --set secrets.jwtSecret="your-random-secret" \
   --set secrets.jwtRefreshSecret="your-different-random-secret"

{{- end }}

{{- if .Values.postgresql.bundled }}
{{- if eq .Values.postgresql.auth.password "" }}

🚨 CRITICAL: PostgreSQL password is not set!
   Please set a custom value:

   --set postgresql.auth.password="strong-random-password"

{{- end }}
{{- end }}

{{- if and .Values.redis.bundled .Values.redis.auth.enabled }}
{{- if eq .Values.redis.auth.password "changeme-redis-password" }}

🚨 CRITICAL: You are using default Redis password!
   Please set a custom value:

   --set redis.auth.password="strong-random-password"

{{- end }}
{{- end }}

{{- if not .Values.fileStorage.enabled }}

⚠️  WARNING: File storage is EPHEMERAL (fileStorage.enabled=false)
   Uploaded files (avatars, attachments) live on per-pod disk and will be
   LOST on pod restart. This is only safe with a single backend replica.
   To persist uploads across restarts (and to run more than one backend
   replica), set fileStorage.enabled=true and configure a ReadWriteMany-
   capable backend (fileStorage.nfs.enabled=true or fileStorage.storageClassName).

{{- end }}

{{- if .Values.fileStorage.enabled }}

ℹ️  NOTE: The uploads PVC accessMode is {{ include "semaphore-chat.fileStorage.accessMode" . }}
   (auto-detected from backend.replicaCount/backend.autoscaling, or an
   explicit fileStorage.accessMode override). PVC accessModes are IMMUTABLE
   in Kubernetes — if you are upgrading a release whose uploads PVC was
   already created with a different accessMode (older chart versions always
   used ReadWriteMany), this install will keep using the PVC's existing
   accessMode, not the one shown above. To actually change it, set
   fileStorage.accessMode explicitly to match what you want and either keep
   it consistent going forward, or delete and let Helm recreate the PVC
   (this destroys any files that were only stored there).

{{- if and (eq (include "semaphore-chat.fileStorage.accessMode" .) "ReadWriteMany") (not .Values.fileStorage.nfs.enabled) (eq .Values.fileStorage.storageClassName "") }}

⚠️  WARNING: fileStorage is set to ReadWriteMany, but neither
   fileStorage.nfs.enabled nor an explicit fileStorage.storageClassName is
   configured — the PVC will fall back to your cluster's default
   StorageClass, which is RWO-only on most providers (EBS, GCP PD, Azure
   Disk, local-path, minikube hostpath). The PVC may stay Pending forever.
   Set fileStorage.nfs.enabled=true or fileStorage.storageClassName to an
   RWX-capable storage class.

{{- end }}

{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 NEXT STEPS

1. Check deployment status:
   $ kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

2. View logs:
   Backend:  $ kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=backend -f
   Frontend: $ kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=frontend -f

3. For production deployments:
   - Enable autoscaling (backend.autoscaling.enabled=true)
   - Configure resource limits
   - Set up monitoring and alerting
   - Configure TLS/SSL (ingress.tls.mode=cert-manager or manual)

4. Documentation:
   📖 https://github.com/semaphore-chat/semaphore-chat/tree/main/helm/semaphore-chat

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Happy chatting! 🎉
