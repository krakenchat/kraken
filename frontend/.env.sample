# Kraken Frontend Environment Variables
# Copy this file to .env for local development

# Backend API URL (for development, this proxies through Vite)
# In production, nginx will proxy /api to the backend service
VITE_API_URL=/api

# WebSocket URL for Socket.IO connection
# For local development with docker-compose
VITE_WS_URL=http://localhost:3000

# In production (Kubernetes), this will be set to the backend service URL
# Example: VITE_WS_URL=https://kraken.example.com
